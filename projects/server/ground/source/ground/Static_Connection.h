#pragma once

namespace vineyard {
  class Ground;

  namespace database {
    class Database;

    class Connection;
  };

  class Static_Connection {
  public:
//      Static_Connection(Ground & ground);
//      ~Static_Connection();
  };
}