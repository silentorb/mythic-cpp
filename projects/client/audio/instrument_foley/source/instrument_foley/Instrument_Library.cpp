#include "Instrument_Library.h"

using namespace std;
using namespace foley;

namespace instrument_foley {

  unique_ptr<Sound> Instrument_Library::get_sound(int id) const {
//    if (id >= instruments.size())
//      throw runtime_error("Invalid sound/instrument id: " + to_string(id) + ".");
//
//    return unique_ptr<Sound>(new Sound(*instruments[id]));
  }
}