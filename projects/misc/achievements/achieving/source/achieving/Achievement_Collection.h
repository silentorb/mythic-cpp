#pragma once

#include <map>
#include <string>
#include "Achievement.h"

namespace achieving {

//  using Achievement_Definition_Collection = std::map<std::string, Achievement_Definition>;
  using Achievement_Collection = std::map<std::string, Achievement>;
}