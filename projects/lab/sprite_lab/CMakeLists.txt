
create_library(sprite_lab)

require(glass logger haft glow typography drawing texturing)
require(mythic)
require(shading)
require(modeling)
require(perspective)
require(lookinglass)
require(client)

add_resources(resources)
